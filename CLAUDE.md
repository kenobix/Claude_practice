# CLAUDE.md

このファイルはリポジトリ全体に適用される、Claude Codeの作業ルールです。

## リポジトリ概要

Claude Codeの学習・実践リポジトリ。日付ディレクトリ（例: `260521`, `260630`, `260804`）ごとに1つの作業セッション・成果物を管理する。各ディレクトリには基本的に `README.md` または `work_log.md` を置き、何を行い何が分かったかを記録する。公開URLは `https://kenobix.github.io/Claude_practice/`（GitHub Pages）。

## Git運用ルール

- **作業ごとにpushする**: 1つの日付ディレクトリでの区切りのよいタスクが完了するたびに、コミット＋ `git push origin main` を行う。既存のコミット履歴のように、小さく頻繁なコミットを基本とする。
- **タスク完了を待たずこまめにpushする**: 複数ステップからなる作業（学習ロードマップの各ステージ実装など）では、ステージ全体の完了を待たず、スクリプト1本・ドキュメント1節など動作確認できたまとまりができた時点で都度コミット＋pushする。
- pushする前に `git status` で意図しない未追跡ファイル・変更が残っていないか確認する。出力物（画像・ログ等）を残す場合はコミットするか `.gitignore` に追加するかを都度判断し、放置しない。
- APIキーなどの秘密情報は絶対にコミットしない。`.env` の実体は `.gitignore` 対象にし、共有するのは `.env.example` のみとする。

## 静的サイトを作る際のホスティング判断基準

新しくWebページ・Webアプリを作る／既存のものを見直す際は、以下の基準で GitHub Pages / Netlify / Vercel を選ぶ。

1. **100%静的（サーバー処理・秘密情報が不要）** → **GitHub Pages**。このリポジトリの既定。追加設定・追加コストが不要。
2. **サーバーレス関数が必要、またはAPIキーやプロンプトなど秘匿したい情報がある、またはNext.jsなどSSRフレームワークを使う** → **Vercel**。このリポジトリはReact中心のスタックが多いため、Netlifyより優先する（プレビューデプロイの完成度が高く、Next.jsとの相性も良い）。
3. **Vercel固有機能に縛られたくない、またはNetlify独自機能（フォーム等）を使いたい静的寄りの案件** → **Netlify**。Vercelとほぼ同等の代替として扱ってよい。

現状の公開ページ一覧・プロジェクトごとの移行要否の詳細分析は [260805/hosting_policy.md](./260805/hosting_policy.md) を参照。新しいページを公開したときはこの判断基準に沿って選択し、`README.md` の公開ページ一覧と `260805/hosting_policy.md` を更新する。

### Vercel/Netlify CLIについてのメモ

`vercel`/`netlify` CLIは初回の `login` のみブラウザでのOAuth認証が必要。一度ログインしトークンが手元にある状態になれば、以降のデプロイ・環境変数設定・関数管理はBashから非対話的に実行できる。
